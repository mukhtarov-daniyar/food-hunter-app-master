import {StyleSheet} from 'react-native';
import {scale} from '../../theme/dimension';

export const headerIconSize = scale(23);

const styles = StyleSheet.create({
  iconContainer: {
    marginRight: scale(10),
  },
});

export default styles;
